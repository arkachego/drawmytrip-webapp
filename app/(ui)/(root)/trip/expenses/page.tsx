'use client';

import { Card } from "@mantine/core";

const ExpensesPage: React.FC = () => {

  return (
    <Card withBorder={true} shadow="md">Expenses Page</Card>
  );

};

export default ExpensesPage;
